import React from 'react';
import { Outlet } from 'react-router-dom';

const Pay = () => {
   return (
      <>
         <h1>Page concernée au paiement</h1>
      </>
   );
};

export default Pay;
